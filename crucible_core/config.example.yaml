# config.example.yaml
# Rename to config.yaml and adjust paths for your local environment.
# The Crucible - Project Chimera 通用配置中心

# Oligo FastAPI（scripts/start_oligo.py）监听地址；Astrocyte 请在 provider_config.json 的 oligo_base_url 或环境变量 OLIGO_BASE_URL 指向同一服务
# 仅本机：127.0.0.1；局域网/远程：可改为 0.0.0.0 并在客户端填写机器 IP
oligo:
  host: "127.0.0.1"
  port: 33333

# --- Generic Crucible ---
vault_root: "D:/Obsidian/Exocortex"  # MUST be absolute
inbox_folder: "D:/Obsidian/Exocortex/00_Inbox/Chimera_Triage"  # MUST be absolute
# 可选：论文 PDF 入库目录；默认 {vault_root}/02_Assets/Papers（启动时自动创建）
# vault_assets_dir: "D:/Obsidian/Exocortex/02_Assets/Papers"

# Optics：Lens YAML/JSON 目录；也可用环境变量 CHIMERA_LENSES_DIR（默认与 crucible.core.platform.get_chimera_root()/lenses 一致）
# lenses_dir: "D:/Obsidian/ChimeraLenses"

# LLM 单次请求的最大等待时间（秒）
# 建议值：60-120（快速模型），120-180（思考链模型）
default_llm_timeout_seconds: 90

# LLM 默认配置（可选覆盖；未配置时使用 Settings 内置默认）
# default_llm_base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
# default_llm_model: "qwen3-235b-a22b"

# --- [PaperMiner Settings] Optional sub-block (flat keys also supported for backward compat) ---
paper_miner:
  arxivpdf_dir: "papers/arxivpdf"      # Will be anchored to PROJECT_ROOT
  md_papers_raw_dir: "papers/md_papers_raw"
  md_papers_dir: "papers/md_papers"
  filtered_dir: "papers/filtered"
  arxiv_query: "cat:cs.AI AND (all:memory OR all:agent)"
  arxiv_max_results: 50
