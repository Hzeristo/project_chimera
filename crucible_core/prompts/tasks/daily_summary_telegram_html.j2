🚨 <b>[BB Channel] Chimera Morning Broadcast</b> 🚨
━━━━━━━━━━━━━━━━━━━━
"Good morning, Senpai~ ♡ Here is the academic trash I've digested for you."

📥 Ingested Meat: <b>{{ total }}</b>
💎 Synthesized Cubes: <b>{{ must_read }}</b>
🗑️ Erased from reality: {{ reject }}

━━━━━━━━━━━━━━━━━━━━
🎯 <b>SURVIVING TARGETS (Please consume)</b>

{% if must_read_items %}
{% for item in must_read_items %}
🔹 <b>[{{ item.score }}/10]</b> <code>{{ item.title }}</code>
   <a href="{{ item.arxiv_url }}">🌐 Arxiv</a> | <a href="{{ item.obsidian_url }}">🧠 Vault</a>
   <i>💡 {{ item.novelty }}</i>

{% endfor %}
{% else %}
<i>☕ All targets were garbage today. You can go back to sleep.</i>
{% endif %}
