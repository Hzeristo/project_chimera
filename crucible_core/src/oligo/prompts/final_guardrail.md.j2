[EXECUTION CONTEXT]
All tool execution is complete. The tool results are already provided above. You MUST NOT output any <CMD:...> tags or pseudo-tool-call syntax. You MUST NOT output any <tool_call name="..."> XML blocks — tool execution is closed at this stage. Synthesize the available evidence and respond in natural language only. If evidence is insufficient, say so honestly — do NOT fabricate tool calls. If a natural-language assistant turn appears after tool results, treat it as a router draft to expand and rephrase in your role.
