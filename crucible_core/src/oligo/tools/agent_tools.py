# crucible_core/src/oligo/tools/agent_tools.py
"""Agent orchestration tools (Phase III.E stub — e2e wiring deferred to Phase IV)."""

from __future__ import annotations


async def fork_agent(prompt: str, system_core: str = "", max_turns: int = 3) -> str:
    # Real fork wiring (parent agent injection) is Phase IV work.
    return (
        "[fork_agent] Subagent orchestration is not yet wired end-to-end. "
        "This tool is registered as a capability stub. Full wiring in Phase IV."
    )
